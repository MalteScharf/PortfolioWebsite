{
  "ignoreCommand": "sh -c 'echo \"Branch: $VERCEL_GIT_COMMIT_REF\"; if [ \"$VERCEL_GIT_COMMIT_REF\" != \"main\" ] && [ \"$VERCEL_GIT_COMMIT_REF\" != \"staging\" ]; then echo \"Skipping build for $VERCEL_GIT_COMMIT_REF\"; exit 0; fi'"
}
